# 测试环境
spring:
  config:
    activate:
      on-profile: test

server:
  port: ${SERVER_PORT:8080}

# 数据库（测试）—— 请按实际测试库修改；可用环境变量覆盖敏感项
yunqi:
  datasource:
    host: ${YUNQI_DB_HOST:localhost}
    port: ${YUNQI_DB_PORT:5432}
    database: ${YUNQI_DB_NAME:zhishu_integrable_framework_test}
    username: ${YUNQI_DB_USERNAME:postgres}
    password: ${YUNQI_DB_PASSWORD:12345678}
    params: sslmode=disable
    pool:
      maximum-pool-size: 30
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  captcha:
    enabled: true
  login-crypto:
    enabled: true

logging:
  level:
    root: INFO
    cn.datafuturex.zhishu: INFO

jwt:
  secret: ${JWT_SECRET:zhishu-test-secret-key-change-me-must-be-256bits-min}
